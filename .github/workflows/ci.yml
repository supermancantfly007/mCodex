name: CI

on:
  workflow_dispatch:
  push:
    branches: [main, master]
  pull_request:

jobs:
  test:
    strategy:
      matrix:
        os: [windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22.12.x
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type-check
        run: npm run typecheck

      - name: Run tests
        run: npm test -- --run

      - name: Build
        run: npm run build

      - name: Validate macOS launchers and deployment examples
        if: runner.os == 'macOS'
        run: |
          zsh -n scripts/manage-macos.sh scripts/start-codex-cdp-macos.sh scripts/manage-docker.sh scripts/install-macos-control-app.sh deploy/examples/start-public-tunnel.sh.example
          osacompile -o "$RUNNER_TEMP/mCodex Control.app" scripts/mcodex-control.applescript.in
          plutil -lint deploy/examples/com.mcodex.public-tunnel.plist.example

      - name: Audit production dependencies
        run: npm audit --omit=dev
